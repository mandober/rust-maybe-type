
/*
pub trait Clone: Sized {
    fn clone(&self) -> Self;

    fn clone_from(&mut self, source: &Self) { ... }
}
*/

// impl<'a, T> Clone for Maybe<&'a T> {
//   /// Maps Maybe<&T> to Maybe<T> by cloning the contents
//   fn clone(&self) -> Self {

//   }
// }
